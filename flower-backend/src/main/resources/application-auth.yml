# 카카오 OAuth: SDK access token 흐름으로 전환 — client-id/secret은 더 이상 서버에서 필요 없음.
# 앱이 직접 SDK로 access token을 받아 서버에 보내면, 서버는 그 토큰으로 카카오 user-info만 조회.

jwt:
  secret: ${JWT_SECRET}
  access-token-validity-seconds: ${JWT_ACCESS_SECONDS:86400}
  refresh-token-validity-seconds: ${JWT_REFRESH_SECONDS:604800}
